// state constants
const START_STATE = "start";
const RUN_STATE = "run";
const DONE_STATE = "done";

// test mode constants
const TIMER_MODE = "timer";
const WORDS_MODE = "words";

export {
    START_STATE,
    RUN_STATE,
    DONE_STATE,
    TIMER_MODE,
    WORDS_MODE
}